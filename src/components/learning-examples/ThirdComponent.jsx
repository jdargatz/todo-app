import React from 'react';

//don't need component import because this is a function
function ThirdComponent() {
    return (
      <div className="thirdComponent">
        another one of them things
      </div>
    );
  }

export default ThirdComponent;